{
	"name": "apathy",
	"version": "0.0.1",
	"description": "Apathy",
	"type": "module",
	"main": "./dist/index.js",
	"types": "./dist/index.d.ts",
	"scripts": {
		"benchmark": "pnpm build && pnpx concurrently -k -s first \"node ./examples/benchmark/simple.js\" \"pnpx autocannon -c 100 -d 30 -p 10 localhost:3000/\"",
		"build": "tsc --build",
		"build:watch": "tsc --build --preserveWatchOutput --watch",
		"clean": "tsc --build --clean"
	},
	"keywords": [],
	"author": "",
	"license": "ISC",
	"devDependencies": {
		"@types/node": "^20.10.0",
		"regexparam": "^2.0.1",
		"typescript": "^5.3.2"
	}
}
